export function toUpperCase(str) {
  return str.toUpperCase()
}

export default {
  last(arr) {
    return arr[arr.length - 1]
  },
  first(arr) {
    console.log('first-----flag')
    return arr[0]
  }
}
